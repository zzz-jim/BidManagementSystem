@model ScientificResearch.ViewModel.ERPNWorkToDoViewModel

@{
    Layout = "~/Areas/Education/Views/Shared/_PopUpLayout.cshtml";
}

<script src="~/Scripts/Custom/forms.js"></script>
<script type="text/javascript">
    $(function () {
        $("#Reported").click(function () {
            var url2 = "/Education/GraduateStudent/AddBookCostRegist?flag=Reported";
             //alert(url2)
            submitForm("form", url2);
        })
    });

    $(document).ready(function () {
        $("#Date1574376052").kendoDatePicker({
            format: "yyyy-MM-dd",
            value: new Date()
        });
        $("#Date880874772").kendoDatePicker({
            format: "yyyy-MM-dd",
            value: new Date()
        });
       
        $("#Date1574376052").attr("readonly", true);
        $("#Date880874772").attr("readonly", true);
    });

    //$(function () {
    //    function show() {
    //        var mydate = new Date();
    //        var str = "" + mydate.getFullYear() + "年";
    //        str += (mydate.getMonth() + 1) + "月";
    //        str += mydate.getDate() + "日";
    //        return str;
    //    }
    //    $(".in").val(show());
    //});
    //上报成功的提示
    $(function () {
        var weatherHidenCreatBtn = "@ViewBag.SendUpSuccess";
        if (weatherHidenCreatBtn == "true") {
            alert("上报成功！");
            parent.location.reload();
        }
    });

  
</script>

<script>
    $(function () {
        $("#Num1099703810").blur(function () {
            if (parseInt($("#Cost").val())<parseInt($("#Num1099703810").val()))
            {
                alert("输入值不能大于书本费,请重新操作")
            }
            else
            {
            //alert( $("#Total").val());
            var sum = parseInt($("#Cost").val()) - parseInt($("#Num1099703810").val());
            $("#Num601643139").val(sum);
            }
        })
    })
    $(function () {
        $("#Num601643139").blur(function () {
            var sum = parseInt($("#Cost").val()) - parseInt($("#Num1099703810").val());
            $("#Num601643139").val(sum);
        })
    })
    
</script>
<input type="hidden" id="Cost" name="Cost" value="@ViewBag.bookCost" />

@*///Num1099703810  书本费   Num601643139  书本费余额*@


@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal widget-body">
        <input type="hidden" id="Freeze" value="@Model.ApplicationId" />
        <table class="table">
            @*@Html.ValidationSummary(true)*@
           
            @Html.HiddenFor(model => model.NWorkToDoID)

            @Html.HiddenFor(model => model.WorkName)

            @Html.HiddenFor(model => model.ApplicationId)

            @Html.HiddenFor(model => model.FormID)

            @Html.HiddenFor(model => model.WorkFlowID)

            @Html.HiddenFor(model => model.UserName)

            @Html.HiddenFor(model => model.TimeStr)

            @Html.HiddenFor(model => model.ShenPiYiJian)

            @Html.HiddenFor(model => model.JieDianID)

            @Html.HiddenFor(model => model.JieDianName)

            @Html.HiddenFor(model => model.ShenPiUserList)

            @Html.HiddenFor(model => model.OKUserList)

            @Html.HiddenFor(model => model.StateNow)

            @Html.HiddenFor(model => model.LateTime)

            @Html.HiddenFor(model => model.WenHao)

            @Html.HiddenFor(model => model.BeiYong1)

            @Html.HiddenFor(model => model.BeiYong2)

            @Html.HiddenFor(model => model.FuJianList)


            @Html.Raw(Model.FormContent)
            @Html.HiddenFor(model => model.FormContent)
            @Html.ValidationMessageFor(model => model.FormContent)

            <tr>
                <td colspan="4" style="vertical-align:middle; text-align:center;">
                    <input type="submit" value="登记" id="Reported" name="Reported" class="btn btn-blue" />
                </td>
            </tr>
        
        </table>
    </div>
}

